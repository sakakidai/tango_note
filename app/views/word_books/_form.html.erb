<%= form_with(model: word_book, local: true) do |form| %>
  <% if word_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word_book.errors.count, "error") %> prohibited this word_book from being saved:</h2>

      <ul>
        <% word_book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
